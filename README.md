# ScienceOS Integration Test

A test repository for validating ScienceOS GitHub integration — branch switching, push/pull, and file sync.

## Structure

```
main/                   ← stable LaTeX paper draft
├── README.md
├── main.tex
├── sections/
│   ├── introduction.tex
│   ├── methods.tex
│   └── conclusion.tex
├── data/
│   ├── results.csv
│   └── parameters.txt
└── references.bib
```

## Branches

- `main` — LaTeX paper draft (this branch)
- `experiment` — Python simulation codebase (very different structure)
